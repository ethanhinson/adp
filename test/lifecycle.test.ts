import {describe,expect,it} from "vitest"; import {assertTransition,canTransition,isTerminal} from "../src/lifecycle.js";
describe("lifecycle",()=>{it("accepts normal and resume paths",()=>{expect(canTransition("submitted","accepted")).toBe(true);expect(canTransition("working","input_required")).toBe(true);expect(canTransition("input_required","working")).toBe(true)});it("rejects illegal transitions",()=>{expect(()=>assertTransition("completed","working")).toThrow();expect(canTransition("submitted","completed")).toBe(false)});it("identifies terminal states",()=>{expect(isTerminal("failed")).toBe(true);expect(isTerminal("input_required")).toBe(false)})});
