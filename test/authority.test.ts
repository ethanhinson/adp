import {describe,expect,it} from "vitest"; import {intersectAuthority,isAuthoritySubset} from "../src/authority.js"; import type {Authority} from "../src/types.js";
const auth=(p:Partial<Authority>={}):Authority=>({capabilities:["shell","test"],filesystem:{read:["/repo"],write:["/repo"]},network:{mode:"any"},subprocess:{allowed:true,commands:["npm"]},credentials:[{handle:"secret:a",scopes:["read"]}],sideEffects:["issue.comment"],delegation:{allowed:true,maxDepth:3},...p});
describe("authority",()=>{it("attenuates read/write and runtime policy",()=>{const child=intersectAuthority(auth(),auth({filesystem:{read:["/repo"],write:[]}}),auth({capabilities:["test"]}),auth());expect(child.filesystem.write).toEqual([]);expect(child.filesystem.read).toEqual(["/repo"]);expect(child.capabilities).toEqual(["test"])});it("cannot create network or credentials",()=>{expect(isAuthoritySubset(auth({network:{mode:"bounded",destinations:["api.example"]}}),auth({network:{mode:"none"}}))).toBe(false);expect(isAuthoritySubset(auth({credentials:[{handle:"secret:b"}]}),auth())).toBe(false)});it("can prohibit further delegation",()=>{const child=intersectAuthority(auth(),auth({delegation:{allowed:false,maxDepth:0}}),auth(),auth());expect(child.delegation).toEqual({allowed:false,maxDepth:0});expect(isAuthoritySubset(child,auth())).toBe(true)})});
